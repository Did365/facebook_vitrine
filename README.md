# Facebook Vitrine

Petit projet web en PHP réalisé pour l’apprentissage.

## 📁 Structure du projet
- `index.php` : page principale
- `about.php` : page à propos
- `contact.php` : page contact
- `register.php` : page d'inscription
- `style.css` : styles CSS
- `db.php` : configuration base de données (publique)
- `db.local.php` : configuration locale (ignorée par Git)

## ⚙️ Installation (local)
1. Installer XAMPP
2. Placer le projet dans `htdocs`
3. Créer une base de données MySQL
4. Configurer `db.local.php`

## 🔐 Sécurité
- `db.local.php` est ignoré via `.gitignore`
- Aucune donnée sensible sur GitHub

## 🚀 Technologies
- PHP
- HTML / CSS
- MySQL

## 👤 Auteur
Did365
